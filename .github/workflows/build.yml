name: Build Shivzo APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      # गिटहब खुद ही सारे फोल्डर्स और कोडिंग फाइलें अंदर ही अंदर बना लेगा
      - name: Auto Create App
        run: |
          mkdir -p app/src/main/java/com/shivzo/app
          mkdir -p app/src/main/res/layout
          
          # settings.gradle
          echo "include ':app'" > settings.gradle
          
          # app/build.gradle (Gradle 9.x के अनुकूल आसान कोडिंग)
          cat << 'EOF' > app/build.gradle
          plugins {
              id 'com.android.application' version '8.4.0'
          }
          android {
              namespace 'com.shivzo.app'
              compileSdk 34
              defaultConfig {
                  applicationId "com.shivzo.app"
                  minSdk 21
                  targetSdk 34
                  versionCode 1
                  versionName "1.0"
              }
          }
          dependencies {
              implementation 'androidx.appcompat:appcompat:1.6.1'
          }
          EOF

          # AndroidManifest.xml
          cat << 'EOF' > app/src/main/AndroidManifest.xml
          <manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.shivzo.app">
              <uses-permission android:name="android.permission.INTERNET" />
              <application android:label="Shivzo" android:theme="@style/Theme.AppCompat.Light.NoActionBar" android:usesCleartextTraffic="true">
                  <activity android:name=".MainActivity" android:exported="true">
                      <intent-filter>
                          <action android:name="android.intent.action.MAIN" />
                          <category android:name="android.intent.category.LAUNCHER" />
                      </intent-filter>
                  </activity>
              </application>
          </manifest>
          EOF

          # activity_main.xml
          cat << 'EOF' > app/src/main/res/layout/activity_main.xml
          <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android" android:layout_width="match_parent" android:layout_height="match_parent">
              <WebView android:id="@+id/webview" android:layout_width="match_parent" android:layout_height="match_parent" />
          </LinearLayout>
          EOF

          # MainActivity.java
          cat << 'EOF' > app/src/main/java/com/shivzo/app/MainActivity.java
          package com.shivzo.app;
          import android.os.Bundle;
          import android.webkit.WebView;
          import android.webkit.WebViewClient;
          import androidx.appcompat.app.AppCompatActivity;
          public class MainActivity extends AppCompatActivity {
              @Override
              protected void onCreate(Bundle savedInstanceState) {
                  super.onCreate(savedInstanceState);
                  setContentView(R.layout.activity_main);
                  WebView webView = findViewById(R.id.webview);
                  webView.getSettings().setJavaScriptEnabled(true);
                  webView.getSettings().setDomStorageEnabled(true);
                  webView.setWebViewClient(new WebViewClient());
                  webView.loadUrl("https://shivzo.in");
              }
          }
          EOF

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build APK
        run: gradle :app:assembleDebug

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Shivzo-APK
          path: app/build/outputs/apk/debug/app-debug.apk
